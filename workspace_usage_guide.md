# FanghaoManus Demo Workspace 使用指南

## 概述
FanghaoManus Demo 增强版是一个多功能智能体系统，支持浏览器操作、文件管理、系统命令、数据处理和网络工具。所有用户生成的文件将自动保存到 `workspace/` 目录，确保系统安全和文件组织。

## 快速开始

### 1. 环境准备
```bash
# 激活conda环境
conda activate OpneManus

# 进入项目目录
cd D:\fanghao\gihub\manusdemo20260118

# 运行主程序（会自动创建workspace目录）
python main.py
```

### 2. 首次运行
程序首次运行时会自动创建以下目录结构：
```
workspace/
├── files/      # 用户生成的文件
├── logs/       # 日志文件
├── data/       # 数据文件
├── outputs/    # 输出文件
└── temp/       # 临时文件
```

## 核心功能

### 1. 文件操作（自动保存到workspace）

#### 创建文件
```
你: 创建一个测试文件，内容为"Hello World"
```
文件将自动保存到：`workspace/files/test.txt`

#### 读取文件
```
你: 读取README.md文件
```
可以读取项目目录中的任何文件

#### 列出工作区
```
你: 列出工作区目录
```
显示workspace目录结构

### 2. 系统操作

#### 获取系统信息
```
你: 查看系统信息
```
返回系统平台、CPU、内存、磁盘等信息

#### 执行安全命令
```
你: 列出当前目录
```
在workspace目录中执行安全命令

#### 执行Python代码
```
你: 计算1到100的和
```
在安全沙箱中执行Python代码

### 3. 数据处理

#### JSON处理
```
你: 验证JSON格式：{"name": "测试", "value": 123}
```
支持JSON解析、验证、查询、格式化

#### 文本处理
```
你: 统计文本："这是一个测试文本"
```
支持文本统计、搜索、替换、分割

#### 数学计算
```
你: 计算圆的面积，半径是5
```
支持基本运算、统计、单位转换

### 4. 网络工具（需要网络连接）

#### HTTP请求
```
你: 获取百度首页
```
发送HTTP请求并返回响应

#### 网络测试
```
你: 测试连接到8.8.8.8
```
测试网络连通性

## 安全特性

### 1. 文件安全
- 所有用户生成的文件自动保存到workspace目录
- 文件大小限制：读取≤10MB，写入≤5MB
- 禁止访问系统关键目录

### 2. 命令安全
- 危险命令过滤：禁止rm、format、shutdown等
- 超时限制：默认30秒
- 输出限制：防止输出过大

### 3. 网络安全
- 本地网络限制：禁止访问localhost、127.0.0.1等
- SSL验证：默认启用
- URL格式验证

### 4. 代码安全
- 沙箱环境执行Python代码
- 限制危险操作（import、eval、exec等）
- 资源使用限制

## 使用示例

### 示例1：创建配置文件
```
你: 创建一个配置文件config.json，内容为{"app": "demo", "port": 8080, "debug": true}
```
文件保存到：`workspace/files/config.json`

### 示例2：系统监控报告
```
你: 获取系统信息并保存到report.txt
```
1. 获取系统信息
2. 保存到workspace/files/report.txt

### 示例3：数据处理流程
```
你: 创建数据文件，然后统计信息
```
1. 创建data.txt文件
2. 读取文件内容
3. 统计文本信息
4. 保存统计结果

## 高级功能

### 1. 工具使用统计
程序会自动记录工具使用情况：
- 调用次数
- 成功率
- 平均执行时间
- 最后使用时间

### 2. 权限控制
可以在初始化时限制可用工具：
```python
# 只允许特定的工具
agent = Agent(
    tools=all_tools,
    allowed_tools=["file_read", "system_info", "http_request"],
    max_tool_usage=100  # 限制总使用次数
)
```

### 3. 资源管理
程序退出时会自动清理资源：
- 关闭浏览器实例
- 清理临时文件
- 释放网络连接

## 故障排除

### 常见问题

#### 问题1：API连接失败
**症状**：程序无法连接到LLM API
**解决**：
1. 检查 `config/config.toml` 中的API密钥
2. 确保 `base_url` 包含 `/v1` 路径
3. 检查网络连接

#### 问题2：文件保存失败
**症状**：文件没有保存到workspace
**解决**：
1. 检查workspace目录权限
2. 确保磁盘有足够空间
3. 检查文件大小是否超过限制

#### 问题3：工具执行错误
**症状**：工具返回错误信息
**解决**：
1. 查看错误信息中的具体原因
2. 检查参数格式是否正确
3. 确保有必要的依赖

### 调试建议

1. **查看详细日志**：程序会显示详细的执行日志
2. **检查workspace目录**：验证文件是否正确保存
3. **观察统计信息**：查看工具使用情况
4. **分步测试**：将复杂任务分解为简单步骤

## 最佳实践

### 1. 文件管理
- 使用有意义的文件名
- 定期清理workspace/temp目录
- 重要文件备份到workspace/outputs

### 2. 工具使用
- 先测试简单功能，再尝试复杂任务
- 仔细阅读工具描述和参数要求
- 处理工具返回的错误信息

### 3. 安全操作
- 不要尝试绕过安全限制
- 定期检查workspace目录内容
- 注意文件权限设置

### 4. 性能优化
- 减少不必要的工具调用
- 批量处理相关操作
- 及时清理不再需要的资源

## 目录结构说明

### workspace/ 目录
```
workspace/
├── files/      # 用户生成的主要文件
│   ├── config.json
│   ├── report.txt
│   └── data.csv
├── logs/       # 程序运行日志
│   └── app.log
├── data/       # 数据文件
│   └── dataset.json
├── outputs/    # 输出文件
│   └── result.pdf
└── temp/       # 临时文件（自动清理）
```

### 项目目录
```
manusdemo20260118/
├── main.py                    # 主程序
├── agent.py                   # 智能体
├── config.py                  # 配置管理
├── llm.py                     # LLM封装
├── memory.py                  # 记忆模块
├── tool/                      # 工具目录
│   ├── browser.py            # 浏览器工具
│   ├── file.py               # 文件工具
│   ├── system.py             # 系统工具
│   ├── data.py               # 数据工具
│   └── network.py            # 网络工具
├── config/                    # 配置文件
│   └── config.toml           # 主配置文件
├── workspace/                 # 工作区目录（自动创建）
└── 文档文件/
    ├── workspace_usage_guide.md  # 本指南
    ├── workspace_test_cases.md   # 测试用例
    └── quick_workspace_test.py   # 快速测试
```

## 命令参考

### 程序命令
```bash
# 运行主程序
python main.py

# 快速测试
python quick_workspace_test.py

# 查看测试用例
cat workspace_test_cases.md
```

### 交互命令
```
# 退出程序
exit
quit
退出

# 查看帮助
help
帮助

# 列出可用工具
tools
工具列表
```

## 版本信息

- **版本**: 2.0.0 增强版
- **环境**: Conda OpneManus
- **Python**: 3.8+
- **状态**: 稳定可用

## 支持与反馈

### 获取帮助
1. 查看本文档
2. 运行测试用例验证功能
3. 检查错误信息中的建议

### 报告问题
1. 描述具体问题现象
2. 提供复现步骤
3. 附上错误信息截图

### 功能建议
1. 描述使用场景
2. 说明具体需求
3. 提供参考实现（可选）

---

**最后更新**: 2024年  
**维护者**: FanghaoManus Demo Team  

> 提示：建议先运行快速测试确保环境正常，然后从简单任务开始逐步尝试复杂功能。