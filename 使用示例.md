# FanghaoManus Demo 使用示例

## 一、快速开始

### 1. 安装依赖

```bash
cd fanghaomanusdemo
pip install -r requirements.txt
```

### 2. 配置API密钥

编辑 `config/config.toml`，设置你的API密钥：

```toml
[llm]
model = "deepseek-chat"                    # 或 "gpt-4o-mini"
base_url = "https://api.deepseek.com/v1"  # 注意：必须包含 /v1 路径
api_key = "YOUR_API_KEY"                   # 替换为你的API密钥
max_tokens = 8192
temperature = 0.7
api_type = "openai"
```

**重要提示：**
- DeepSeek API的 `base_url` 必须是 `https://api.deepseek.com/v1`（包含 `/v1`）
- OpenAI API的 `base_url` 是 `https://api.openai.com/v1`
- 其他兼容API也需要包含 `/v1` 路径

### 3. 运行程序

```bash
python main.py
```

## 二、常见问题解决

### 问题1: 连接超时 (ConnectTimeout)

**错误信息：**
```
httpx.ConnectTimeout
openai.APITimeoutError: Request timed out.
```

**解决方案：**

1. **检查网络连接**：
   ```bash
   ping api.deepseek.com
   ```

2. **检查 base_url 配置**：
   - ✅ 正确：`https://api.deepseek.com/v1`
   - ❌ 错误：`https://api.deepseek.com`（缺少 `/v1`）

3. **检查API密钥**：
   - 确保API密钥有效
   - 确保API密钥有足够的余额

4. **使用代理（如果需要）**：
   ```python
   # 在 llm.py 中修改
   import httpx
   self.client = AsyncOpenAI(
       api_key=llm_config.api_key,
       base_url=base_url,
       timeout=60.0,
       http_client=httpx.AsyncClient(
           proxies="http://your-proxy:port"  # 添加代理
       )
   )
   ```

### 问题2: API密钥错误

**错误信息：**
```
openai.AuthenticationError: Incorrect API key provided
```

**解决方案：**
1. 检查 `config/config.toml` 中的 `api_key` 是否正确
2. 确保API密钥没有过期
3. 确保API密钥有使用权限

### 问题3: 模型不存在

**错误信息：**
```
openai.NotFoundError: The model 'xxx' does not exist
```

**解决方案：**
1. 检查模型名称是否正确
2. DeepSeek模型名：`deepseek-chat` 或 `deepseek-reasoner`
3. OpenAI模型名：`gpt-4o-mini`、`gpt-4o` 等

## 三、使用示例

### 示例1: 打开网页

```
你: 帮我打开百度首页

助手: 正在打开百度首页...
```

**执行流程：**
1. Agent分析用户请求
2. LLM决定使用 `browser` 工具
3. 调用 `browser.execute(action="go_to_url", url="https://www.baidu.com")`
4. 浏览器导航到百度首页
5. 返回结果给用户

### 示例2: 搜索内容

```
你: 打开百度，搜索"Python教程"

助手:
1. 正在打开百度...
2. 正在输入搜索关键词...
3. 正在点击搜索按钮...
4. 搜索完成！
```

### 示例3: 多步操作

```
你: 打开百度，搜索"天气"，然后告诉我第一个结果的标题

助手:
1. 打开百度
2. 搜索"天气"
3. 获取第一个搜索结果
4. 标题是：...
```

## 四、配置不同API

### DeepSeek API

```toml
[llm]
model = "deepseek-chat"
base_url = "https://api.deepseek.com/v1"
api_key = "YOUR_DEEPSEEK_API_KEY"
api_type = "openai"
```

### OpenAI API

```toml
[llm]
model = "gpt-4o-mini"
base_url = "https://api.openai.com/v1"
api_key = "YOUR_OPENAI_API_KEY"
api_type = "openai"
```

### Azure OpenAI

```toml
[llm]
api_type = "azure"
model = "YOUR_MODEL_NAME"
base_url = "https://YOUR_RESOURCE.openai.azure.com/openai/deployments/YOUR_DEPLOYMENT"
api_key = "YOUR_AZURE_API_KEY"
api_version = "2024-08-01-preview"
max_tokens = 4096
temperature = 0.7
```

### Ollama (本地)

```toml
[llm]
api_type = "ollama"
model = "llama3.2"
base_url = "http://localhost:11434/v1"
api_key = "ollama"
max_tokens = 4096
temperature = 0.7
```

**注意：** 使用Ollama需要先启动Ollama服务：
```bash
ollama serve
```

## 五、调试技巧

### 1. 查看详细日志

在代码中添加日志：

```python
import logging
logging.basicConfig(level=logging.DEBUG)
```

### 2. 测试API连接

```python
# test_api.py
import asyncio
from llm import LLM

async def test():
    llm = LLM()
    response = await llm.ask([{"role": "user", "content": "你好"}])
    print(response)

asyncio.run(test())
```

### 3. 检查浏览器工具

```python
# test_browser.py
import asyncio
from tool.browser import BrowserTool

async def test():
    browser = BrowserTool()
    result = await browser.execute(action="go_to_url", url="https://www.baidu.com")
    print(result)
    await browser.cleanup()

asyncio.run(test())
```

## 六、性能优化

### 1. 减少max_tokens

如果响应慢，可以减少 `max_tokens`：

```toml
max_tokens = 2048  # 从8192减少到2048
```

### 2. 调整temperature

降低temperature可以提高响应速度（但可能降低创造性）：

```toml
temperature = 0.3  # 从0.7降低到0.3
```

### 3. 使用更快的模型

- DeepSeek: `deepseek-chat`（较快）
- OpenAI: `gpt-4o-mini`（较快）vs `gpt-4o`（较慢但更强）

## 七、常见错误及解决方案

| 错误 | 原因 | 解决方案 |
|------|------|---------|
| `ConnectTimeout` | 网络连接超时 | 检查网络、base_url、添加重试 |
| `AuthenticationError` | API密钥错误 | 检查api_key配置 |
| `NotFoundError` | 模型不存在 | 检查model名称 |
| `RateLimitError` | 请求频率过高 | 降低请求频率或升级API |
| `InvalidRequestError` | 消息格式错误 | 检查消息序列（tool消息前必须有assistant消息） |

## 八、最佳实践

1. **API密钥安全**：
   - 不要将API密钥提交到Git
   - 使用环境变量或配置文件（不提交到版本控制）

2. **错误处理**：
   - 代码已包含重试机制（最多3次）
   - 超时时间设置为60秒

3. **资源清理**：
   - 程序退出时会自动清理浏览器资源
   - 也可以手动调用 `agent.cleanup()`

4. **消息管理**：
   - 默认最多保留100条消息
   - 可以通过 `memory.max_messages` 调整

## 九、下一步

1. **添加更多工具**：参考 `tool/browser.py` 添加新工具
2. **自定义系统提示**：修改 `main.py` 中的 `system_prompt`
3. **扩展功能**：添加文件操作、Python执行等工具

## 十、获取帮助

如果遇到问题：
1. 检查配置文件是否正确
2. 查看错误日志
3. 测试API连接
4. 检查网络环境

祝使用愉快！🎉
